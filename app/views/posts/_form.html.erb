<%= form_for @new_post do |f| %>
  <% if @new_post.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@new_post.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @new_post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>  

  <p>
    <%= (t 'posts.author', scope: :custom) %><br>
    <%= current_user.screenname %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
  
  <p>
    <%= f.submit %>
  </p>
<% end %>