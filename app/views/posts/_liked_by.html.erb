<% if liked_by_reference && ! liked_by_reference.empty? %>
	<%= (t 'posts.liked_by', scope: :custom) %>:
	<% liked_by_reference.each do |user| %>
		<%= user.screenname+" " %>
	<% end %>
<% else %>
	<%= 'Be first to like!' %>	  
<% end %>